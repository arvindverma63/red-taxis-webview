# QA Report — 2026-06-10 (Iteration 0: full sweep)

Scope: admin-v2 frontend, .NET backend API, mobile (driver-app + local-sms-v2).
Branch: `claude/red-taxi-admin-qa-check-y8fozw` (includes merge of `origin/dev`).
Fix policy: safe fixes applied; logic-change/security/device-dependent items reported only.

---

## Fixed this iteration (committed)

| # | Area | Finding | Commit |
|---|------|---------|--------|
| 1 | API / WhatsApp | **Operator widget endpoints lost in a merge** — the intelligence PR (`17ce253`) overwrote `WhatsAppAgentController` and dropped: transfer/close/transcript routes, filtered conversation list, the NOT_OWNER 403 ownership check on operator send, Pusher events (`whatsapp-message` on send, `whatsapp-transfer`, `whatsapp-closed`, `whatsapp-returned-to-ai`), and senderName persistence. The admin-v2 widget still called all of them (404s in production; live sync broken; any operator could send on any conversation). Handlers still existed — routes restored verbatim from the original widget PR (`70ce173`). Resolves both PR #34 items in issue #48. | `12a04fe` |
| 2 | Mobile / driver-app | **Wrong password logged the user out** — a 401 from `/UserProfile/Login` triggered the full AuthGate logout (stop foreground service, clear prefs, navigation reset, loud Sentry event). Login URL now excluded from the 401→logout path. (Issue #48 PR #47 P2.) Needs device verification before release. | `f648f73` |
| 3 | Mobile / local-sms-v2 | **Bearer token + SMS content logged to logcat on release builds** — `HttpLoggingInterceptor.Level.BODY` unconditionally. Now BODY on debug only, NONE on release, `Authorization` header redacted. | `f648f73` |
| 4 | CI | **Backend tests have never run in CI** — only `RedTaxi.API.csproj` was restored, so `dotnet test` died in 0s with NETSDK1004 on every run, and `continue-on-error: true` hid it. CI now restores/builds the full solution so test compile errors surface and tests execute. Also removed 4 stale `.claude/worktrees/*` gitlink entries (broken submodule refs erroring CI checkout cleanup) and gitignored the directory. | `d51272e` |
| 5 | admin-v2 | **ESLint errors**: refs written during render in `use-pusher-notifications.ts` (notification mute prefs could be stale/torn under concurrent rendering), 2× setState-in-effect in zone editor (cascading renders), 5× unescaped JSX entities, 2 unused imports. Production build verified clean after fixes. | `1648e98` |

## Verified already fixed (issue #48 items — no action needed)

- **PR #18** WhatsApp payload parsing — bookingId now parsed from the last `_` segment (`ReceiveWhatsAppReply.cs:188`).
- **PR #30** Cached tariff drift — fast path now recomputes via `TariffPriceHelper.Calculate` from live tariff + cached miles (`AiAgentController.cs:362-371`).
- **PR #47 P1** GPS 401 background-isolate crash — `AuthGate._bounceToLogin` null-checks the navigator and logs instead of crashing (`auth_gate.dart:96-105`).
- **PR #40** FirebaseInitProvider — present in `AndroidManifest.xml:67`.
- **PR #40/#43** Firebase project mismatch — backend (`RedTaxi.API/pushNotificationConfig.json`), Android (`google-services.json`) and iOS plist all now on `red-taxi-push` (aligned by the dev-branch store-release commits). Caveat: drivers still on old APKs hold tokens for the old project until they update.

## Still open — needs Peter's decision (logic changes, NOT fixed)

1. **PR #26 — caller route filter OR vs AND** (`CallerMappingPipeline.cs:167-175`): routes match if pickup==home **OR** destination∈top-destinations. AND would narrow suggestions; test against real data first. Side note: the filter serialises each dest entry to JSON and re-parses it *per route* inside the Where — works, but wasteful; worth a cleanup when the OR/AND decision lands.
2. **PR #24 — AI create-booking account number** (`AiAgentController.cs:1158-1174`): `CreateBookingRequestDto.AccountNo` is never set (defaults 0) with `Scope = Cash`. Issue #48 says cash bookings need 9999 (or tenant cash account). Recommend: read from tenant config, fall back 9999.
3. **PR #22 P2 — duplicate screen pops** (`CallEventsController.cs`): `ringing`, `in-progress` **and** `completed` all trigger `CallNotification` — an operator can get 3 pops per call. Recommend: per-CallSid dedup (in-memory dictionary like `_processedSids` in the WhatsApp controller).
4. **PR #21 P2 — test-whatsapp writes phantom DriverAllocation rows** (`SettingsController.cs` test endpoint) — still calls the full send flow.
5. **PR #18 P2 — delivery log matched by phone, not message SID** (now `Features/Messaging/UpdateOutboundMessageStatus.cs:58-62`): matches NotificationLog by recipient+status; two queued messages to the same driver can update the wrong row. Recommend: store/match the gateway message id.
6. **local-sms-v2 — SMS confirmation timeout assumes success** (`SmsSender.kt:36-48`): no sent-broadcast within 15s is treated as sent. Can mask real failures (esp. Android 13+). Recommend: report "unknown" status distinct from "sent".

## Security — deferred per Peter's instruction (verified, unchanged)

- Live Twilio auth token still in `staging/start-api.cmd:53` (rotate after scrubbing — it's in git history).
- `GET /api/v2/call-events/stt-compare` still `[AllowAnonymous]` (`CallEventsController.cs:134`).
- `IntelligenceController` now has **26** `[AllowAnonymous]` endpoints (the issue flagged 5 — it grew). Pipeline runs + tenant data exposed unauthenticated in production.
- Dev routes still gated `Development || Staging` (`Program.cs:648`).
- New finding, same class: hardcoded AI agent number `+441747441405` in `CallEventsController` (tenant-specific value in code — belongs in tenant config).

## Test/tooling status

- **Backend build**: clean (0 errors, 989 pre-existing nullable warnings — known cosmetic debt).
- **Backend tests**: not runnable in this container or CI — `ApiFixture` boots the real API against a seeded Postgres dev DB (none here; CI has none). CI now at least builds + executes them; failures stay non-blocking (`continue-on-error`) until a seeded test DB exists. **Recommendation**: containerised Postgres + seed script in CI, then make tests blocking.
- **admin-v2**: build ✅, tsc ✅; lint at 49 errors / 70 warnings remaining (down from 58/72) — bulk is the ref-write-during-render pattern across ~24 files, queued in the worklist.
- **Mobile**: static review only (no Flutter/Android SDK in container). Both fixes need device verification.

## Process findings

- `dev` was 6 commits ahead of `main` (driver-app store release prep, May 4–11) — merged into this branch so QA ran against current code. Nothing on `main` is newer than `dev`.
- CLAUDE.md "Current State" says WhatsApp widget endpoints/ownership were complete — they were, then a merge lost them (finding #1). Worth a docs note once this PR merges.

## Iteration 5 addendum (merge-loss cross-check)

Diffed all files touched by `17ce253` against `70ce173`. Beyond the controller
endpoints (fixed in iteration 0), one more regression: **the catch block in
`MessageService.SendWhatsAppMessage` was emptied** — it previously logged and
rethrew. With the empty catch, Twilio failures were silently swallowed, the
`SendWhatsAppMessage` handler logged "sent" and returned success, and the
notification audit recorded a send that never happened. Restored log + rethrow
(`MessageService.cs:88`). Everything else 17ce253 deleted was the intentional
DB→in-memory route-cache upgrade.

## Iteration 6 addendum (controller hygiene + AllowAnonymous inventory)

**V2 controller hygiene:** effectively clean. Only genuine deviation is
`DeliveryStatusController` constructing a tenant `DbContext` inline (~line
335) for Twilio callbacks — unavoidable tenant resolution for unauthenticated
webhooks, but the block belongs in a handler like `ReceiveWhatsAppReply` does
it. Refactor candidate, not a bug. (PlansController/SettingsController hits
were false positives.)

**AllowAnonymous inventory (31 total):**

| Endpoint | Verdict |
|---|---|
| `POST /api/v2/call-events/incoming` | ✅ Justified — `[ValidateTwilioRequest]` signature check |
| `POST /api/v2/whatsapp-agent/incoming` | ✅ Justified — `[ValidateTwilioVoice]` |
| DeliveryStatusController Twilio callbacks | ✅ Justified — `[ValidateTwilioRequest]` |
| `StripeWebhookController` | ✅ Justified — Stripe signature (`ConstructEvent`) |
| `GET /api/v2/plans` | ✅ Justified — public pricing for the landing page |
| `GET /api/v2/call-events/stt-compare` | ❌ Known issue #48 item — no auth, triggers paid STT + mutates aliases |
| `IntelligenceController` — **26 endpoints** | ❌ Known issue #48 item (grew from 5) — pipeline runs, location/route CRUD, tenant data |
| `POST /api/v2/availability/reminder` | ❌ **NEW** — anonymous, gated only by hardcoded query key `"ace@taxis"` (`AvailabilityController.cs:69-74`). Weak shared secret in code + URL (logged by proxies), and a tenancy-rule violation (tenant-specific literal). Recommend `[Authorize]` or move the key to env/tenant config. NOT fixed (security deferred). |

## Iteration 7 addendum (driver-app static audit)

- **Removed**: 268-line commented-out old ProfileScreen implementation at the
  top of `lib/screens/profile_screen.dart` (pure comments — zero compile risk).
- **Resolved on dev already**: the third-party logger endpoint default
  (`acetaxislogger.backendcodersindia.com`) is gone — `REDTAXI_LOG_ENDPOINT`
  now defaults to empty.
- **Documented, not changed** (no Flutter SDK here to compile-verify; mobile
  needs device testing):
  - 12 silent `catch (_)` blocks: `gps_log_file.dart` ×6 (best-effort file IO
    around the GPS JSONL log), `log_helper_laravel.dart` ×2 (logging infra —
    logging inside would recurse), `api_helper.dart:202`,
    `shared_pref_helper.dart:114`, `main.dart:27`,
    `count_booking_provider.dart:136`. Most look intentionally best-effort;
    recommend a `debugPrint` in the api_helper and shared_pref ones when the
    app is next on a device-verified release cycle.
  - `baseUrl` default `https://ace-server.1soft.co.uk/api` was set
    deliberately by the dev-branch store-release commit ("correct production
    API base URL") — flagging only because CLAUDE.md names
    `api.redtaxi.co.uk` as production; confirm which is canonical for the
    driver app.

## Iteration 8 addendum (local-sms-v2 static audit)

Clean. The flagged "QR config error swallowing" was a false alarm — parse and
camera-init failures are logged and surfaced to the user; the only silent
catch is dispose-time camera cleanup (`QrScannerScreen.kt:601`), which is
appropriate. WorkManager polling is a documented 15-minute safety net behind
the foreground loop, by design. Remaining notes: release builds minify with
R8 but `mapping.txt` isn't archived and the app has no crash reporter —
release stack traces can't be deobfuscated; recommend archiving the mapping
per release (or adding Sentry) when convenient. The timeout-assumes-sent
behaviour in `SmsSender.kt` remains in the needs-decision list.

## Iteration 9 addendum (pattern compliance + CI verify — worklist complete)

**Pattern compliance (checked app-wide, not sampled):** all four mechanical
rules pass across every page — zero `<Input type="date">`, zero const
`pageSize` (all state), every Recharts chart `<Tooltip>` carries
`chartCursor` (the two flagged files use shadcn hover-tooltips, not chart
tooltips), and no raw `£${...}`/`toFixed` money formatting outside
`fmtMoney`/`toLocaleString`.

**CI:** green on the branch (run #11, both jobs). The Test step executes the
suite (~15s) — failures are visible in the log and stay non-blocking until CI
has a seeded database.

**Worklist complete.** Loop is now in re-sweep mode: re-running builds/lint,
watching PR #53 and issue #48, until told to stop.

## Decisions implemented (2026-06-10, Peter's answers)

All 6 needs-decision items resolved and implemented:

1. **Caller routes AND** — `CallerMappingPipeline` now requires pickup = home
   AND destination ∈ top destinations (also replaced the per-route JSON
   re-parse with a HashSet). Worth a sanity check of suggestions after the
   next pipeline run.
2. **AI cash bookings** — `AccountNumber = 9999` hardcoded in the create-booking
   tool (Peter's choice over tenant config).
3. **Screen pops: ringing only** — `CallNotification` fires only on the
   `ringing` status; `completed` still accepted for the STT comparison.
   Note: a Dev Phone setup that only sends `completed` will not pop.
4. **test-whatsapp** — `isTest` flag skips the DriverAllocation insert; the
   real send, audit log and balance decrement are unchanged.
5. **Delivery log by message id** — `QueueOutboundSms` returns the queued id,
   stamped as `ExternalId` on the NotificationLog row; the status handler
   matches by it first, phone fallback only for legacy rows (`ExternalId == null`).
6. **SMS timeout → failed** — local-sms-v2 reports failed when Android gives
   no send confirmation in 15s. Backend never re-fetches failed messages, so
   no duplicate-SMS risk; failures surface in the delivery log. *Needs device
   verification before release.*

## Re-sweep 1 (post-decisions)

- CI green on the decision-implementation commit (`73f0d23`) and predecessors.
- Ran the full **DB-free unit test set locally (198 tests)** — found 2
  pre-existing failures in `IntelligencePipelineTests.Normalize_CleansAddress`:
  stale expectations from before house-number stripping and apostrophe
  normalisation were added (they survived because CI never ran tests).
  Expectations updated to the documented behaviour; 198/198 now pass.
- Coverage note: the restored WhatsApp operator endpoints and today's decision
  changes have **no snapshot coverage** (WhatsApp tests are rate-limiter unit
  tests only; AI agent snapshots cover auth failures only) — so no snapshot
  diffs are expected on the dev machine, but new tests are recommended once a
  seeded test DB exists.

## Sweep 2, iteration 1 (backend hygiene + dependencies + secrets)

**Fixed:**
- **10 `async void` messaging methods → `async Task`** with explicit `_ =`
  fire-and-forget at all 14 call sites. async void exceptions can crash the
  process; now they surface as logged unobserved-task exceptions with
  identical timing semantics. (MAUI legacy module left as-is — superseded.)
- **WatchDog.NET removed** — referenced for ONE log line (UserProfileService),
  middleware never registered, dashboard non-functional. It transitively
  dragged in 12 of the 13 vulnerable NuGet packages found by
  `dotnet list package --vulnerable`, including all 3 Criticals (LiteDB,
  System.Drawing.Common 4.7.0, System.Text.Encodings.Web 4.5.0). The one log
  call now uses the service's ILogger; Dapper (which WatchDog supplied
  transitively to the legacy AuthenticationService) added explicitly at the
  same version. Post-fix scan: **1 vulnerable package remains**.

**Verified clean:**
- All 297 MediatR handlers have `Log.ForContext("Feature", ...)` — full pass.
- Hardcoded-credentials grep across `src/`: only a test dummy.

**Reported:**
- **AutoMapper 12.0.1 (High, GHSA-rvv3-g6hj-g44x)** — actively used across
  services; the fix is the v13+ migration (DI extensions package merged into
  core). Small migration, recommend a dedicated PR.
- **`staging/start-api.cmd` contains ~12 live secrets**, not just the Twilio
  token issue #48 flagged: JWT_SECRET_KEY, CLERK_SECRET_KEY, RESEND_API_KEY,
  FCM, Pusher, Google Places, IdealPostcodes, GetAddress, SMS/Webex. All in
  git history. When the deferred security pass happens, scrub the file AND
  rotate the full set, not only Twilio.

## Sweep 2, iteration 2 (frontend apps + Clerk security patch + EF sync)

- **CRITICAL security patch**: Clerk advisories GHSA-vqx2-fgx2-5wq9
  (middleware route-protection bypass) + GHSA-w24r-5266-9c3c (authorization
  bypass) affected the installed versions in **all four apps** — including
  the production admin panel. In-range patched everywhere; npm audit reports
  zero Clerk advisories now. All four builds verified, admin-v2 lint clean,
  saas-admin vitest 8/8. **Vercel picks these up when PR #53 merges.**
- **Real bug found by saas-admin's own test suite**: `Footer.footer_text`
  missing from the en locale (present in fr) — English landing footer
  copyright never rendered. Fixed; 8/8 tests.
- headless-dispatch + account-booker production builds clean.
- saas-admin build verified clean (needs the 2 Clerk env vars, as on Vercel).
- **EF migration sync: PASS** — probe migration against RedTaxiDbContext was
  empty; model matches the snapshot.
- Remaining npm moderates (non-Clerk) left for routine dependency updates.

**Sweep 2 complete.**
