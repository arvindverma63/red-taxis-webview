# Dispatch v2 — Implementation Plan

Spec: `docs/superpowers/specs/2026-06-10-dispatch-v2-design.md`
Branch: `feature/dispatch-v2`

## Phase 1 — Backend additive v2 endpoints (TDD)
1. Write `RedTaxi.Tests/SnapshotTests/DispatchV2SnapshotTests.cs` covering the new endpoints
   (read endpoints snapshot; mutating endpoints error/validation cases).
2. Add routes (all delegating to existing handlers, envelope helpers):
   - V2 `DispatchController`: soft-allocate, soft-allocate/confirm-all, arrived
   - V2 `BookingsController`: complete, duration, merge, turndown, coa (POST/GET),
     {id}/confirmation-text, {id}/payment-link, {id}/payment-reminder, {id}/refund,
     {id}/receipt, send-quote
   - V2 `PricingController`: quote (mirrors v1 GetPrice routing verbatim — flagged)
   - V2 `CallEventsController`: caller-lookup
   - V2 `MessagingController`: sms
   - V2 `POIController`: search
   - V2 `AddressController`: dispatch-search, dispatch-resolve, postcode-lookup
   - V2 `UserProfileController`: by-username, availability
3. `dotnet build` clean (no new warnings) + run DB-free test suite.

## Phase 2 — Frontend app scaffold
1. Copy `headless-dispatch` → `dispatch-v2` (done), drop Syncfusion deps, add vitest. (done)
2. Vitest config + setup. (done)

## Phase 3 — v2 API layer (TDD)
1. `src/utils/apiV2.js` — envelope adapter (`v2Get`/`v2Post`/`v2Put`) + tests.
2. Rewrite `src/utils/apiReq.js` on the adapter; identical exports/shapes + tests per function.
3. AuthContext iframe path → `/api/v2/users/by-username`.

## Phase 4 — Scheduler component (TDD)
1. `components/Scheduler/eventVisuals.js` (+tests) — pure port of onEventRendered.
2. `components/Scheduler/layout.js` (+tests) — overlap columns, time math.
3. `DayView.jsx`, `AgendaView.jsx`, `DispatchScheduler.jsx` (+tests).
4. Swap into `pages/Scheduler.jsx`; remove Syncfusion imports/licence/CSS.

## Phase 5 — Integration & verification
1. `npm run test`, `npm run lint`, `npm run build` green in dispatch-v2.
2. `headless-dispatch` untouched (git diff confirms).
3. Backend build + tests green.
4. Update CLAUDE.md current state; commit, push, draft PR.
